# SQL Analysis: Sales Dataset

**Description:** Set of SQL queries demonstrating aggregation, date grouping, and ranking.

**Files**
- `analysis_queries.sql` — queries for total revenue, monthly trends, top customers, orders with high discounts

**Usage**
Run these queries in any SQL environment (MySQL/Postgres). Adjust `DATE_FORMAT` if using different SQL dialect.

**Author:** Shreya R Kinnal — github.com/<your-username>
